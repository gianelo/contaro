# Local Postgres started by `pnpm db:up` (see docker-compose.yml).
DATABASE_URL=## Local Postgres started by `pnpm db:up` (see docker-compose.yml).
AUTH_SECRET=# AUTH_SECRET=          # pnpm dlx auth secret --- IGNORE ---
AUTH_GOOGLE_ID=# AUTH_GOOGLE_ID=    # pnpm dlx auth google --- IGNORE ---
AUTH_GOOGLE_SECRET=# AUTH_GOOGLE_SECRET= # pnpm dlx auth google --- IGNORE ---
