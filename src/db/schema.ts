/**
 * The Drizzle schema. Tables arrive with the tickets that need them:
 * Spaces and Members with #4, Categories with #6, Movements with #7,
 * Budgets with #10. The skeleton only establishes that the pipeline runs.
 */
export {};
